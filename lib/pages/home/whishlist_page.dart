import 'package:flutter/material.dart';
import 'package:shamo/theme.dart';

class WhishlistPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Whishlist Page', style: primaryTextStyle,),
    );
  }
}
